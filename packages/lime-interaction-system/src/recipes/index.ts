/**
 * Reusable interaction choreography.
 *
 * A recipe is a semantic surface composition — what happens to the surfaces. It is not a
 * renderer (that is physical execution) and not a scenario (that is a deterministic sequence
 * used to exercise behaviour).
 */
export * from "./surfaces.ts";
export * from "./ride.ts";

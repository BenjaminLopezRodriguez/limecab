/** Base --radius: 1rem (globals.css:45). Roles below are the recurring usages. */
export const radius = {
  mapLabel: 12,
  chip: 16,
  control: 20,
  card: 24,
  /** Sheet top corners + panels. Source: ui/drawer.tsx:125, service-sheet.tsx:204. */
  sheet: 28,
  pill: 9999,
} as const;

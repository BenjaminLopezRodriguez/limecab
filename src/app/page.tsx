/**
 * Home is the ride flow, and the ride flow is mounted by the root layout so a
 * half-chosen destination survives a trip to another tab. So this route has
 * nothing of its own to render.
 */
export default function Home() {
  return null;
}
